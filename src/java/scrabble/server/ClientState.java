package scrabble.server;

// An enum representing the current state of the client
public enum ClientState {
    // Client is connected or waiting for a game
    CONNECTED,

    // Client is in a game
    IN_GAME
}
